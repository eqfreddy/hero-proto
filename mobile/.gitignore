node_modules/
# iOS platform — generated by `npx cap add ios`; build on CI via Codemagic / GitHub mac runners
ios/
# Firebase credential files — add these after platform init, never commit
google-services.json
GoogleService-Info.plist
# Android release keystore — keep out of version control
*.keystore
*.jks
# Android build artifacts
android/app/build/
android/build/
android/.gradle/
android/local.properties
